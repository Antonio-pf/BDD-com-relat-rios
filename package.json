{
  "scripts": {
    "test": "dotnet test Calculator.Test.csproj",
    "allure:generate": "npx allure generate -o allure-report",
    "allure:clean": "rm -rf allure-results allure-report && npm run test && npm run allure:generate",
    "allure:open": "npx allure open allure-report",
    "allure:serve": "npx allure serve allure-results",
    "report": "npm run test && npm run allure:generate && npm run allure:open"
  },
  "dependencies": {
    "allure": "^3.0.0"
  }
}
